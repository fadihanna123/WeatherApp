import { StyleSheet } from 'react-native';

export const weatherFormStyles = StyleSheet.create({
  inputbox: {
    flexDirection: 'row',
    justifyContent: 'center',
  },
  textinput: {
    borderWidth: 1,
    padding: 10,
    width: '100%',
  },
});
