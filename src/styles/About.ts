import { StyleSheet } from 'react-native';

export const aboutStyles = StyleSheet.create({
  aboutTextBig: {
    color: '#fff',
    fontSize: 35,
    textAlign: 'center',
    textShadowColor: '#ff0000',
  },
  aboutTextSmall: {
    color: '#fff',
    fontSize: 20,
    textAlign: 'center',
  },
});
