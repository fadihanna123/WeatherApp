import { StyleSheet } from 'react-native';

export const aboutStyles = StyleSheet.create({
  aboutTextBig: {
    color: '#fff',
    fontSize: 40,
    textAlign: 'center',
    textShadowColor: 'red',
  },
  aboutTextSmall: {
    color: '#fff',
    fontSize: 20,
    textAlign: 'center',
  },
});
